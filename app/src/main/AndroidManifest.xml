<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pe.edu.upc.bunker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".modelViews.activities.CreateSpaceActivity"></activity>
        <activity android:name=".modelViews.activities.SignUpActivity" />
        <activity android:name=".modelViews.activities.NavigationActivity" />
        <activity android:name=".modelViews.activities.LoginActivity" />
        <activity android:name=".modelViews.activities.OnBoardingActivity" />
        <activity android:name=".modelViews.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>